Useful commands from repo root:
- `hugo --panicOnWarning --noBuildLock --cleanDestinationDir` build the site
- `hugo server --bind 127.0.0.1 --noBuildLock` run local dev server
- `git status` inspect working tree
- `rg PATTERN .` search project text
- `ls`, `pwd` basic navigation
Beads note from repo AGENTS: use Beads, and if `bd` fails with cold-start `port 0` / circuit-breaker issue, run `bd dolt start` before retrying.