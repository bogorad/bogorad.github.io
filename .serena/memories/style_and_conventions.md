Keep changes surgical and follow existing Hugo template and TOML structure. Site-level config stays minimal in `hugo.toml`. Homepage content belongs in `content/_index.md`, not config. Contact icons are configured in `data/contacts.toml` and rendered by `layouts/_partials/home/contact-icon.html`, which has special inline SVG branches for `signal`, `substack`, and `x-twitter`; all other icons use Font Awesome class names. Edit generated output only through Hugo source files.